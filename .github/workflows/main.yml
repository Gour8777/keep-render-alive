name: Keep Render Alive

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backends
        run: |
          curl -m 10 https://cc-backend-ei7t.onrender.com/
          curl -m 10 https://finance-backend-lnok.onrender.com/
